import { DayFormattingPipe } from './day-formatting.pipe';

describe('DayFormattingPipe', () => {
  it('create an instance', () => {
    const pipe = new DayFormattingPipe();
    expect(pipe).toBeTruthy();
  });
});
